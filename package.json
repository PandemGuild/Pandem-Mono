{
  "name": "pandem-project",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@pandem/contracts\" \"npm run dev --workspace=@pandem/backend\" \"npm run dev --workspace=@pandem/frontend\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
